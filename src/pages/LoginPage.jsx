import { LockKeyhole, Mail, Store } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { supabase, supabaseEnabled } from '../lib/supabase'

export default function LoginPage(){const [email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const nav=useNavigate();const submit=async e=>{e.preventDefault();if(!supabaseEnabled){nav('/owner');return}setBusy(true);const {error}=await supabase.auth.signInWithPassword({email,password});setBusy(false);if(error)return alert(error.message);nav('/owner')};return <div className="auth-page"><div className="auth-card"><Link to="/" className="auth-brand"><span>iM</span><div><b>iMersWAStore</b><small>Owner Area</small></div></Link><h1>Masuk ke Dashboard</h1><p>Kelola produk, inventory, HPP, profit report dan branding toko.</p><form onSubmit={submit}><label><span><Mail/> Email</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="owner@toko.com"/></label><label><span><LockKeyhole/> Password</span><input type="password" required={supabaseEnabled} value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label><button className="btn-primary full large" disabled={busy}>{busy?'Masuk...':supabaseEnabled?'Masuk':'Buka Demo Dashboard'}</button></form>{!supabaseEnabled&&<div className="auth-note">Mode demo aktif karena <code>.env</code> Supabase belum diisi.</div>}<Link to="/" className="back-store"><Store/> Kembali ke toko</Link></div></div>}
