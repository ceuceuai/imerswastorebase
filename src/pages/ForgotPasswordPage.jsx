import { ArrowLeft, Mail } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useState } from 'react'
import { supabase, supabaseEnabled } from '../lib/supabase'
export default function ForgotPasswordPage(){const[email,setEmail]=useState('');const[busy,setBusy]=useState(false);const[done,setDone]=useState(false);const submit=async e=>{e.preventDefault();if(!supabaseEnabled)return alert('Supabase belum dikonfigurasi.');setBusy(true);try{const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});if(error)throw error;setDone(true)}catch(e){alert(e.message)}finally{setBusy(false)}};return <div className="auth-page auth-page-v2"><div className="auth-card"><div className="auth-brand"><span>iM</span><div><b>iMersWAStore</b><small>Password Recovery</small></div></div><h1>Lupa Password</h1><p>Masukkan email akun. Link reset password akan dikirim melalui Supabase Auth.</p>{done?<div className="auth-success">Link reset sudah dikirim. Periksa inbox/spam email Anda.</div>:<form onSubmit={submit}><label><span><Mail/> Email</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="email@contoh.com"/></label><button className="btn-primary full large" disabled={busy}>{busy?'Mengirim...':'Kirim Link Reset'}</button></form>}<div className="auth-links"><Link to="/login"><ArrowLeft size={14}/> Kembali ke Login</Link></div></div></div>}
