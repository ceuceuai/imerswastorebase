import { LockKeyhole } from 'lucide-react'
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { supabase, supabaseEnabled } from '../lib/supabase'
export default function ResetPasswordPage(){const[p1,setP1]=useState('');const[p2,setP2]=useState('');const[busy,setBusy]=useState(false);const nav=useNavigate();const submit=async e=>{e.preventDefault();if(p1.length<6)return alert('Password minimal 6 karakter.');if(p1!==p2)return alert('Password tidak sama.');if(!supabaseEnabled)return;setBusy(true);try{const{error}=await supabase.auth.updateUser({password:p1});if(error)throw error;alert('Password berhasil diperbarui.');nav('/login')}catch(e){alert(e.message)}finally{setBusy(false)}};return <div className="auth-page auth-page-v2"><div className="auth-card"><div className="auth-brand"><span>iM</span><div><b>iMersWAStore</b><small>New Password</small></div></div><h1>Buat Password Baru</h1><p>Gunakan password minimal 6 karakter dan jangan gunakan password yang mudah ditebak.</p><form onSubmit={submit}><label><span><LockKeyhole/> Password Baru</span><input type="password" required value={p1} onChange={e=>setP1(e.target.value)}/></label><label><span><LockKeyhole/> Ulangi Password</span><input type="password" required value={p2} onChange={e=>setP2(e.target.value)}/></label><button className="btn-primary full large" disabled={busy}>{busy?'Menyimpan...':'Simpan Password Baru'}</button></form></div></div>}
